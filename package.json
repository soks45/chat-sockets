{
  "name": "socket-chat",
  "version": "1.0.0",
  "description": "simple chat based on socket connection",
  "main": "index.js",
  "scripts": {
    "start": "npm i && concurrently \"npm run client\" \"npm run server\"",
    "client": "cd ./sockets-client && npm i && npm run start",
    "server": "cd ./sockets-server && npm i && npm run start"
  },
  "author": "soks",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.2.1"
  }
}
